# Smoke Fixture Two

This second fixture is opened together with the first to confirm that multiple
Markdown documents open as tabs without crashing.

```java
final class Example {
    String message() {
        return "code block renders";
    }
}
```

- Second document
- Opened as a separate tab
