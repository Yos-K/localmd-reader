# 探索セッション: purchase (2026-06-13)

## チャーター

| 項目 | 内容 |
|------|------|
| 対象 | purchase クラスタ（`domain-glossary-purchase.md`：課金フロー・購入結果・UI状態・表示判定・Play Billing 境界） |
| 観点 | 収益/安全に直結する規則（PUR1–PUR6）の境界と沈黙領域。特に未承認購入・null/不正値の fail-closed・キャッシュの鮮度 |
| 動機 | T2（自己操縦シグナルの唯一の未探索クラスタ）。`exploration-status.sh` で purchase は未探索。これで6クラスタ全踏査 |
| タイムボックス | probe 23 本 |

実行環境: 全対象が domain 層で **Android 非依存**のため、純 JVM の scratch コード（`/tmp/purchaseprobe/Probe.java`）で観測した。package-private な状態判定は見えないため、`ProPurchasePresentation.from(Free, ui).messageCode()` を UI 状態の観測レンズに用いた。

## probe と観測

| # | 突いた規則/沈黙領域 | probe | 観測 | 判定 | 振り分け |
|---|------|------|------|------|------|
| 1 | PUR1（安全） | 未承認 `purchased(false)` | `pending` / **Free** | 規則どおり | （既存テスト有） |
| 2 | PUR1 | 承認済 `purchased(true)` | `purchased` / Pro | 規則どおり | （既存テスト有） |
| 3–5 | PUR1 | pending / billingUnavailable / notPurchased | 各 Free | 規則どおり | （既存テスト有） |
| 6 | fail-closed | `fromPersistenceCode(null)` | `unknown` / Free | 規則どおり | — |
| 7 | fail-closed | `fromPersistenceCode("garbage")` | `unknown` / Free | 規則どおり | （既存テスト有: raw-token→unknown） |
| 8 | 信頼境界（沈黙） | `fromPersistenceCode("purchased")` | `purchased` / **Pro**（Billing 再照会なし） | 規則が沈黙 | ② glossary |
| 9 | L1 | `ProPurchaseUiState.safe(null)` | unavailable | 規則どおり | — |
| 10 | PUR6/fail-closed | `fromPurchaseState(null)` | unavailable | 規則どおり | — |
| 11 | null安全 | `Presentation.from(null,null)` | action=false / msg=purchase_unavailable | 規則どおり | — |
| 12 | キャッシュ鮮度（沈黙） | `restore("purchased", 0L)`（epoch） | **Pro**・`verifiedAtMillis=0`（TTL/失効なし） | 規則が沈黙 | ② glossary + ③ assert |
| 13 | L1 | `verifiedAt(null,5)` / `verifiedAt(purchased,-1)` | 両方 IllegalArgumentException | 規則どおり | （既存テスト有） |
| 14,19 | PUR2/PUR5 | `from(Pro, ready)` / `from(Pro, unavailable)` | 両方 action=false/restore=false/**pro_active**（Pro 短絡） | 規則どおり | （既存テスト確認） |
| 15 | PUR2/PUR5 | `from(Free, ready)` | action=true/restore=true/purchase_available | 規則どおり | — |
| 16–18 | PUR2/PUR5 | `from(Free, inProgress/pending/unavailable)` | 各 action=false・対応メッセージ | 規則どおり | — |
| 20 | PUR3 | `started/pending/unavailable .uiState()` | in_progress / pending / unavailable | 規則どおり | — |
| 21 | 権限 L1 | `allows(free/pro/null)` | free許可・pro拒否(Free)・pro全許可・null→例外 | 規則どおり | — |
| 22 | 永続往復 | 全コード `code→state→code` | 全コード一致 | 規則どおり | — |
| 23 | ファサード | `current(null)` / `currentClosedTestingRelease()` | 両方 Free | 規則どおり | （既存テスト有） |

「判定」は3値: **規則どおり** / **規則と異なる** / **規則が沈黙**。

## 振り分けの結果

- **① issue: 0 件**。purchase クラスタは収益/安全の不変条件（PUR1 未承認→Free・全 null/不正の fail-closed・Pro 短絡）を満たし、既存テストが手厚く固定済みで、**規則と異なる挙動・実害バグは検出されなかった**。これ自体が成果（安全クリティカル領域の健全性を確認）。
- **② glossary**: `domain-glossary-purchase.md` の `ProPurchaseCacheEntry` に**鮮度/信頼境界**を明文化（P8/P12）。エントリ自身は TTL/失効を持たず `verifiedAtMillis` は出所情報のみ、永続コード `"purchased"` は検証済み事実として信頼し再照会しない、鮮度/再検証は呼び出し側ポリシー、不正/null は fail-closed。「なぜ」を実装/方針から裏取り。
- **③ assert**: `ProPurchaseCacheEntryTest` に年齢非依存（no-TTL）の characterization を1件追加（`restore("purchased", 0L)`→Pro）。将来 TTL を入れる変更を意図的・可視にするため。全10テスト green。

## 次のチャーター候補

- 6クラスタ全踏査完了。次サイクルは**相互作用の探索**（単軸→2軸へ。例: SharedPreferences キーを共有する viewer×appearance、または purchase×viewer の Pro 特典表示）。
- purchase の Alloy モデル（`docs/domain/models/pro-purchase-state.als`）と本探索の突合（状態遷移の網羅性検証）。

## 価値評価（機械可読・集計は scripts/exploration-status.sh）

- probes: 23
- findings: 2
- triage-issue: 0
- triage-glossary: 1
- triage-assert: 1
- time-minutes: 40
