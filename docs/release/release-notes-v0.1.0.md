# Release Notes v0.1.0

LocalMD Reader v0.1.0 is the first public release candidate.

## Highlights

- Open local `.md` and `.markdown` files.
- Open Markdown files from Android file managers and other apps.
- Open one or more Markdown files from Termux with `mdlite-reader`.
- Create a temporary Markdown document from clipboard text and save it with
  `Save as...` when needed.
- Read Markdown with tabs, tab restoration, recent files, dark mode, and pinch
  font sizing.
- Keep controls at the top or bottom and switch the app UI between English and
  Japanese.
- Render common Markdown locally: headings, paragraphs, lists, checklists,
  blockquotes, code blocks, inline code, links, tables, and horizontal rules.
- Highlight supported fenced code blocks and render basic Mermaid diagrams locally.
- Keep reading private: no ads, no analytics, no login, no sync, no network
  permission.

## Notes

- LocalMD Reader v0.1.0 is a viewer, not an editor.
- Temporary Markdown documents created from clipboard text are not saved
  automatically.
- Files larger than 10 MB are rejected with a clear message.
- Remote image loading, relative image rendering, advanced Mermaid controls, math, footnotes,
  cloud sync, PDF export, and HTML export are not included in this version.

## Play Console Release Notes

```text
Initial release of LocalMD Reader, a lightweight local Markdown viewer.

- Open .md and .markdown files
- Open files from Android file managers, other apps, or Termux
- Create temporary Markdown from clipboard text and save it when needed
- Read with tabs, tab restoration, recent files, dark mode, and pinch font sizing
- View common Markdown including tables, code blocks, checklists, links, and basic Mermaid diagrams
- No ads, analytics, login, sync, or network permission
```
